to produce dist folder ➜ npx tailwindcss -i ./styles.css -o ./dist/styles.css --watch

to produce release ➜ zip dist, images_optimized, logos svg, index.html and tacholife png

UPDATE:

to produce release ➜ zip dist, assets, contactForm.js, index.html and combined-dark.png, thank-you.html, private-policy.html
